name: Test connectivity to Lightsail

on:
  workflow_dispatch:  # 수동 실행용ㅇㅇ

jobs:
  ping-test:
    runs-on: ubuntu-latest

    steps:
      - name: PING 테스트
        run: |
          echo "🔍 Pinging ${{ secrets.EC2_HOST }}..."
          ping -c 3 ${{ secrets.EC2_HOST }} || echo "❌ Ping failed"
